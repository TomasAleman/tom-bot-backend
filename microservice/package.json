{
  "name": "tom-bot-hot-path",
  "version": "0.1.0",
  "description": "Microservicio dedicado al hot path del bot de reservas (reemplaza n8n cuando supera ~500 msg/min)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "@fastify/helmet": "^12.0.1",
    "@fastify/jwt": "^9.0.1",
    "@fastify/rate-limit": "^10.1.1",
    "@fastify/static": "^8.0.2",
    "bcryptjs": "^2.4.3",
    "fastify": "^5.0.0",
    "ioredis": "^5.4.1",
    "pg": "^8.13.0",
    "undici": "^7.0.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
